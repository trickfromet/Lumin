# Project Orchestrator Workspace

This folder is the working directory for the `teamwork_preview_orchestrator` subagent.
It will contain files like `plan.md`, `progress.md`, and `context.md` representing the implementation strategy and status.
